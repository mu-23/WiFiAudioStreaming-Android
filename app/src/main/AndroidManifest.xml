<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" tools:targetApi="33" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_PROMOTED_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WiFiAudioStreaming"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">

        <!-- Persist AppCompat per-app language selection on Android < 13. -->
        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>

        <!-- Lab: receive the Shizuku binder used to launch our shell-uid UserService. -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:enabled="true"
            android:exported="true"
            android:multiprocess="false"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.WiFiAudioStreaming.Splash">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- One filter, no host: it already covers every wifiaudio:// authority,
                 pairing included, so a second one for host="pair" would only be a
                 duplicate. autoVerify has no meaning on a custom scheme - it applies
                 to http/https app links, which are declared separately below. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="wifiaudio" />
            </intent-filter>

            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="www.marcomorosi.eu"
                    android:pathPrefix="/wifi-audio-streaming/pair" />
                <data
                    android:scheme="https"
                    android:host="www.marcomorosi.eu"
                    android:pathPrefix="/it/wifi-audio-streaming/pair" />
                <data
                    android:scheme="https"
                    android:host="marcomorosi.eu"
                    android:pathPrefix="/wifi-audio-streaming/pair" />
                <data
                    android:scheme="https"
                    android:host="marcomorosi.eu"
                    android:pathPrefix="/it/wifi-audio-streaming/pair" />
            </intent-filter>

            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />

        </activity>

        <!-- Ingresso privato per tile e widget: non esportato apposta, cosi'
             nessun'altra app puo' far partire una connessione client. -->
        <activity
            android:name=".CommandTrampolineActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:taskAffinity=""
            android:theme="@android:style/Theme.NoDisplay" />

        <receiver
            android:name=".StreamingActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.cuscus.wifiaudiostreaming.ACTION_STOP_STREAMING" />
                <action android:name="com.cuscus.wifiaudiostreaming.ACTION_VOLUME_UP" />
                <action android:name="com.cuscus.wifiaudiostreaming.ACTION_VOLUME_DOWN" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".scripting.ScriptCommandReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.cuscus.wifiaudiostreaming.action.START_SERVER" />
                <action android:name="com.cuscus.wifiaudiostreaming.action.CONNECT" />
                <action android:name="com.cuscus.wifiaudiostreaming.action.STOP" />
                <action android:name="com.cuscus.wifiaudiostreaming.action.TOGGLE" />
                <action android:name="com.cuscus.wifiaudiostreaming.action.SET" />
                <action android:name="com.cuscus.wifiaudiostreaming.action.USB" />
            </intent-filter>
        </receiver>

        <service
            android:name=".ClientService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="false"/>

        <!-- Lab foreground host; privileged capture itself runs in Shizuku UserService. -->
        <service
            android:name=".shizuku.ShizukuBridgeHostService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="false" />

        <service
            android:name=".RtpClientService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="false"/>

        <service
            android:name=".SnapcastClientService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="false"/>

        <service
            android:name=".AudioCaptureService"
            android:foregroundServiceType="mediaProjection|microphone"
            android:exported="false" />

        <service
            android:name=".AutoConnectService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

        <service
            android:name=".ServerTileService"
            android:icon="@drawable/ic_preview_server"
            android:label="@string/widget_server_name"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <service
            android:name=".ClientTileService"
            android:icon="@drawable/ic_preview_client"
            android:label="@string/widget_client_name"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <receiver
            android:name="com.cuscus.wifiaudiostreaming.ServerWidgetReceiver"
            android:label="@string/widget_server_name"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/server_widget_info" />
        </receiver>

        <receiver
            android:name="com.cuscus.wifiaudiostreaming.ClientWidgetReceiver"
            android:label="@string/widget_client_name"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/client_widget_info" />
        </receiver>

    </application>

</manifest>